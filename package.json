{
  "name": "password-validator",
  "version": "1.0.0",
  "description": "A simple module to check if a pssword is valid. The length of the passord should be greater or equal than 8 and less or equal than 15",
  "main": "index.js",
  "scripts": {
    "test": "mocha --reporter spec"
  },
  "author": "Okba KHENISSI",
  "license": "ISC",
  "engines": {
    "node": ">=4.2.4"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "mocha": "^5.2.0"
  }
}
